<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TabStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <declare-styleable name="ringButton">
        <attr name="ringbutton_ringWidth" format="dimension" />
        <attr name="ringbutton_ringColor" format="color" />
        <attr name="ringbutton_circleColor" format="color" />
        <attr name="ringbutton_textColor" format="color" />
        <attr name="ringbutton_pressedColor" format="color" />
        <attr name="ringbutton_dividerColor" format="color" />
        <attr name="ringbutton_dividerHighLightColor" format="color" />
        <attr name="ringbutton_dividerShadowColor" format="color" />
        <attr name="ringbutton_textSize" format="dimension" />
        <attr name="ringbutton_upDrawable" format="reference" />
        <attr name="ringbutton_downDrawable" format="reference" />
        <attr name="ringbutton_upText" format="string" />
        <attr name="ringbutton_downText" format="string" />
        <attr name="ringbutton_dividerSize" format="dimension" />
        <attr name="ringbutton_baseColor" format="color" />
    </declare-styleable>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
